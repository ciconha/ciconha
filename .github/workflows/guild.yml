name: guild Workflow

on:
  workflow_dispatch:
    inputs:
      usuario:
        description: "Usuário GitHub"
        required: true
        default: "ciconha"

permissions:
  contents: write

jobs:
  gerar-cartao:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Gerar Cartão Guild
        run: |
          echo "Gerando cartão RPG para ciconha..."
          echo "Estilo: fantasy"
          echo "Layout: card"
          echo "Área: backend"
          echo "XP e Rank calculados automaticamente."
      - name: Commit SVG
        run: |
          git add .
          git commit -m "🎴 Guild Card atualizado"
          git push
